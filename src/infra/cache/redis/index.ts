// import { createClient } from 'redis';

// (async () => {
//   const client = createClient(process.env.REDIS_PORT)

//   client.on('error', (err) => console.log('Redis Client Error', err))

//   // client.set('Teste', 'cache redis', resp => console.log(resp))

//   const chacedData = client.get('Teste', (error, data) => {
//     console.log(data)
//   })

//   console.log(chacedData)
// })()
